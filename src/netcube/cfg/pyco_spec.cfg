
[common]

exactPatternMatch   = boolean(default=False)

maxWait = float(default=2)

discoverPrompt = boolean(default=False)

checkIfOutputComplete = boolean(default=False)

 [[events]]
 

[__many__]

discoverPrompt = boolean(default=None)

 [[events]]
 	
 
 